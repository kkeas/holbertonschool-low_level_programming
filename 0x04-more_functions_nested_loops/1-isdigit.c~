#include "main.h"

/**
 * _isdigit - checks for a digit between 0-9
 * @c: the digit
 *Return: 1 if a digit 0 if not
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= "9")
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
