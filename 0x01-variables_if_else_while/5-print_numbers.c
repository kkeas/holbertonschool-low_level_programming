#include <stdio.h>
/**
 * main - prints single digit numbers from 1-10
 * Return: always zero if exited properly, otherwise non zero
 *
 */

int main(void)
{
int n;

for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");
return (0);
}
