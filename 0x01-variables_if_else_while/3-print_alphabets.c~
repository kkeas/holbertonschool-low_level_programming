#include <stdio.h>

/**
 * main - lists the alphabet in lowercase and uppercase
 * return: always 0 when exited properly, otherwise non zero
 */

int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(a);
for (a = 'A'; a <= 'Z'; a++)
putchar(a);
putchar('\n');
return (0);
}
